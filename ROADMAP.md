# TAO — Roadmap

## Next

- [ ] **Explore and add providers** — Investigate additional LLM providers beyond Claude CLI and Copilot CLI. Evaluate candidates, implement new provider modules following the `LLMProvider` protocol in `src/models.py`.
